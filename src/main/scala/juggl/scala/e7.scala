package juggl.scala

// maps are cool too
object E7 {
  val pairs = Map(
    "one"   -> 1,
    "two"   -> 2,
    "three" -> 3,
    "four"  -> 4
  )

  // println(pairs("four"))  => 4
}
// go back to 5
